package Day3;

// Parnel might call a method static if it only has static methods inside it
public class Outer {
    int a;
    // Place the class here so that it can see some details in the outer class but it can't be seen by other places
    static class Inner {

    }
}
