package interfaces;

/*
This interface has no methods defined at all, it's called a tagging/marker interface and is used to indicate some
kind of boolean condition. By adding the interface it tells the compiler some specific information, such as the
Serializable interface from Java, which is used to tell the compiler if an object can be safely serialised
 */
public interface Methodless {
}
